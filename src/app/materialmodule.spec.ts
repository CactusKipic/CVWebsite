import { MaterialmoduleImport } from './materialmodule-import.module';

describe('Materialmodule', () => {
  it('should create an instance', () => {
    expect(new MaterialmoduleImport()).toBeTruthy();
  });
});
